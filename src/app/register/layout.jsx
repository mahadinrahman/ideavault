export const metadata = {
  title: "Register-IdeaVault",
  description: "If you add an idea or see other interactions,must  register.",
}

export default function ServicesLayout({
  children,
}) {
  return (
    <div >
      {children}
    </div>
  );
}
