export const metadata = {
  title: "Profile-IdeaVault",
  description: "My Profile page where my information are gathered here.",
}

export default function ServicesLayout({
  children,
}) {
  return (
    <div >
      {children}
    </div>
  );
}
