export const metadata = {
  title: "Ideas-IdeaVault",
  description: "All ideas are showed in this page.",
}

export default function ServicesLayout({
  children,
}) {
  return (
    <div >
      {children}
    </div>
  );
}
