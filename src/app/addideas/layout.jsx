export const metadata = {
  title: "AddIdeas-IdeaVault",
  description: "You can add ideas in this page.Then ,it adds in ideas page.",
}

export default function ServicesLayout({
  children,
}) {
  return (
    <div >
      {children}
    </div>
  );
}
